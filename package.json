{
  "name": "amp-codelab",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "run-p start build:dev",
    "build": "html-includes --src src --dest dist --minify",
    "build:dev": "html-includes --src src --dest dist --watch",
    "start": "http-server dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-includes": "^4.3.3",
    "http-server": "^0.12.3",
    "npm-run-all": "^4.1.5"
  }
}
